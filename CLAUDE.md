# Infra

Personal homelab infrastructure-as-code. See `CONTEXT.md` for domain language and `docs/adr/` for architectural decisions.

## Agent skills

### Issue tracker

GitHub Issues on `giacomoguidotto/infra`. See `docs/agents/issue-tracker.md`.

### Triage labels

Default vocabulary (`needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix`). See `docs/agents/triage-labels.md`.

### Domain docs

Single-context repo. `CONTEXT.md` + `docs/adr/` at the root. See `docs/agents/domain.md`.
